const path = require("path");

const configPath = path.join(__dirname, './config.json');

// 全部的源语言
const SocureLanguage = {

}
// 全部的目标语言
const TargetLanguage = {

}

module.exports = {
    configPath,
    SocureLanguage,
    TargetLanguage
}